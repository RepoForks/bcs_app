<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:orientation="vertical"
    android:layout_height="wrap_content">

    <include
        android:id="@+id/tool_bar"
        layout="@layout/tool_bar"></include>

    <Button
        android:id="@+id/btnOCR"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:background="@drawable/fancy_green_button"
        android:textColor="#ffffff"
        android:text="Recognize text on picture"/>

    <com.daimajia.slider.library.SliderLayout
        android:id="@+id/slider"
        android:layout_marginTop="10dp"
        android:layout_width="fill_parent"
        android:layout_height="200dp"
        />
    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="20dp">
        <Button
            android:id="@+id/btnDelete"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:textColor="#ffffff"
            android:text="Delete this picture"
            android:background="@drawable/fancy_red_button"
            android:layout_height="match_parent"
            android:layout_marginLeft="5dp"
            android:layout_marginRight="5dp"/>

        <Button
            android:id="@+id/btnNextPicture"
            android:layout_weight="2"
            android:layout_width="0dp"
            android:textColor="#ffffff"
            android:text="Next picture"
            android:background="@drawable/fancy_blue_button"
            android:layout_height="match_parent"
            android:layout_marginRight="5dp"/>
    </LinearLayout>

</LinearLayout>